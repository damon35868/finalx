export enum EnvType {
  rel = 'release',
  pro = 'production',
  dev = 'development',
}

export enum LocalStorageKeys {
  token = 'token',
  userInfo = 'userInfo',
}

export enum RequestMethod {
  post = 'POST',
  get = 'GET',
}
